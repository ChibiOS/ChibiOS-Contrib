AT32 EXINTv1 driver.

Driver capability:

- Support for the EXINT peripheral.

The file registry must export:

AT32_EXINT_INTEN_MASK       - Mask of the fixed lines that must not be
                              configured by the driver (0..31).
